# Server
PORT=3000

# Security — generate with: openssl rand -hex 32
STATE_SECRET=your_random_secret_key_64_chars_or_more

# Allowed redirect hosts (comma separated, no spaces)
ALLOWED_REDIRECT_HOSTS=yourdomain.com,anotherdomain.com
